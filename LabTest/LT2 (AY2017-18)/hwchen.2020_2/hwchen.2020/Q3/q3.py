def get_postal_codes(file_name):
    # modify the code below
    with open(file_name) as dataset:
        t_list = []
        for line in dataset:
            t_list.append(line)
        print()
    return []
